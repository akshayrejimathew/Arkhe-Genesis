'use client';

import Workbench from '@/components/layout/Workbench';

/**
 * WORKBENCH PAGE
 * Main IDE interface routing bridge
 * Imports the complete Workbench component with all tools and panels
 */
export default function WorkbenchPage() {
  return <Workbench />;
}